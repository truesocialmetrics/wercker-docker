build:
  box: truesocialmetrics/ci
  docker: true
  steps:
    - script:
        name: docker-test
        code: |
          docker run --rm --name static-site truesocialmetrics/casperjs casperjs --version
