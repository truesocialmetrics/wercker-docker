build:
  box: truesocialmetrics/ci
  docker: true
  steps:
    - script:
        name: docker-test
        code: |
          docker run --rm --name static-site --network=$DOCKER_NETWORK_NAME truesocialmetrics/casperjs casperjs --version
